version: 2

deploy:
  steps:
    terraformInit:
      before:
      - name: "Get contents of env0.system-env-vars.json"
        run: |
          cat env0.system-env-vars.json
      # - name: "Step 2"
      #   run: |
      #     echo "Command 2"
